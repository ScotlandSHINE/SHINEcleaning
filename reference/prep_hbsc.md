# reshape hbsc summary data

reshape hbsc summary data

## Usage

``` r
prep_hbsc(dat, create_cols = FALSE)
```

## Arguments

- dat:

  HBSC data to pass in

- create_cols:

  copy existing p7, s2 and s4 columns to fill missing years

## Value

long-format dataframe
