# Module

Retrieve and add modules from a vector of files.

## Usage

``` r
remove_modules(files, modules)

get_modules(files, modules)
```

## Arguments

- files:

  JavaScript files

- modules:

  JavaScript files names that require the `type = module`.
