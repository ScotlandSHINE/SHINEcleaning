# Return only numeric values, no warnings

Return only numeric values, no warnings

## Usage

``` r
valid_numbers(x)
```

## Arguments

- x:

  A variable with mixed data types

## Value

A vector of only valid numbers (as numeric)
