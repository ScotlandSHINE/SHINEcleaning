# Input List

Forces the input received from the WebSocket to a list. This should
really not be needed as it is handled like so by default.

## Usage

``` r
leprechaun_handler_list(data)
```

## Arguments

- data:

  Input data received from WebSocket.
