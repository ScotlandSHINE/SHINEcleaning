# createReport Server

createReport Server

## Usage

``` r
createReport_server(id, data)
```

## Arguments

- id:

  Unique id for module instance.

- data:

  reactive dataframe, from cleanUpload_server
