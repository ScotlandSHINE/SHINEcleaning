# Data upload checks

Data upload checks

## Usage

``` r
upcheck_status(data)
```

## Arguments

- data:

## Value

single row data.frame of columns fail (logical), message (character),
level (integer)
